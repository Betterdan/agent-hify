# Agent 产品落地方法论

> 版本：v0.4（草案）｜ 日期：2026-06-29
> 性质：记录 `agent-hify` 立项与实现过程中**实际走过的方法论**，整理成可复用流程。
> 归属：方法论由项目所有者定义与演进；本文是对已发生过程的归纳，随实践**定时更新**。
> 配套：[DESIGN.md](./DESIGN.md)（应用本方法论的产出）、[CLAUDE.md](./CLAUDE.md)（项目行为指令）

---

## 0. 核心信条

1. **方法论优先于产品**：宁可产品小，也要走通"问题界定 → 设计 → 实现 → 评估 → 迭代"闭环并沉淀。
2. **决策可追溯**：每个取舍记下"为什么"，过程本身即资产。
3. **轮子复用，要害透明**：成熟能力（模型 SDK、文档解析）复用现成库；方法论要害（Agent 决策、检索、评估）手写保持可见可控。
4. **按规模与维护者裁剪**：一切设计回答"目标规模多大、谁来维护"，拒绝过度工程（YAGNI）。

---

## 1. 方法论总流程

```
① 调研标杆 → ② 功能取舍 → ③ 技术选型 → ④ 运维预期 → ⑤ 文字归档
                                                              ↓
                                               ⑥ 架构设计
                                                              ↓
                          ⑦ 工程搭建 → ⑧ 功能实现 → ⑨ 测试 → ⑩ 部署
                                                              ↓
                                    （每阶段评估 → 迭代，回到对应环节）
```

---

## 2. 各阶段详解

### ① 调研标杆
- 选定标杆与同类产品，用结构化框架（本项目用 `competitive-brief`）拆解功能、定位、优劣。
- 产出：功能对比矩阵 + **定位空白/差异化机会**。
- 关键问句：*"哪些已被占满（不能正面做）？哪里是没人做好的空白（我的机会）？"*

### ② 功能取舍
- 拆成**模块 + 子功能**清单；YAGNI 三分法：**砍 / 缓（登记迭代版图）/ 保留(可简化)**。
- 取舍依据两条硬约束：**成功标准**（方法论优先 vs 产品优先）与**目标规模**。
- 原则：*"为规模/高可用的功能先砍，为方法论闭环的功能尽量留。"*

### ③ 技术选型
- 由**成功标准 + 维护者能力**驱动，不为时髦选型。
- 逐层定：Web 框架 / 模型网关 / 存储 / 异步 / RAG / Agent / 前端构建与 UI 库。
- 对维护者不熟的领域额外讲清楚、给对比、降心智负担。贯彻"轮子复用 + 要害手写透明"。

### ④ 运维预期
- 按规模明确：部署形态 / 资源规格 / 持久化 / 备份 / 升级迁移 / 密钥 / 可观测 / 可用性 / 安全 / **容量边界**。
- 区分**生产**与**调试/开发**两套预期。原则：维护者负担最小化。

### ⑤ 文字归档
- 把 ①–④ 的决策链固化为文档（决策可追溯）。
- 文档分离：**设计文档（DESIGN.md）** 与 **方法论文档（本文件）**；竞品调研单独成文。

### ⑥ 架构设计
在决策归档之上，产出以下架构产物（本项目落在 DESIGN.md + CLAUDE.md）：
| 产物 | 内容 |
|---|---|
| 应用架构 | 架构风格（模块化单体）、模块边界、**模块依赖（单向无环）**、数据流向 |
| 代码组织 | 仓库/包结构、每个模块内部分层约定 |
| 部署架构 | 部署拓扑、服务划分、Compose |
| 性能预判 | 并发/延迟/吞吐预算、瓶颈认知、扩展手段 |
| 数据设计 | 表结构、关键数据结构（如 content-blocks）、扩展预埋 |
| 接口规范 | 对外 HTTP API 约定、模块间接口契约、扩展契约 |
| 行为指令 | 给实现者（含 AI agent）的编码与协作规则（落在 CLAUDE.md） |

### ⑦ 工程搭建
- 按代码组织搭骨架：依赖、配置、DB/迁移、容器、前后端脚手架，**一键起**。
- 产出可运行的空壳 + CI/本地命令。

### ⑧ 功能实现
- **分阶段**（地基 → 简单应用 → 能力 → 复杂应用 → 评估深化），每阶段独立走 spec → plan → 实现。
- 阶段排序按**依赖关系**。遵循 TDD。

### ⑨ 测试
- 单元（mock 外部）+ 集成（真实 DB/队列）+ 检索/评估测试。
- 用**评估钩子**做效果验证（评估方法由项目所有者定义）。

### ⑩ 部署
- Compose 上线、迁移、备份、health/日志接入。非高可用起步。
- 上线后回到评估 → 迭代。

---

## 2bis. P0 实战归纳

> 实战纪律详见 [`docs/methodology/learnings-p0.md`](./docs/methodology/learnings-p0.md)。
> 本节不重复内容，保持 METHODOLOGY.md 专注于核心框架。

---

## 3. 在本项目的落点

| 阶段 | 产物 |
|---|---|
| ① 调研 | `docs/competitive-brief-agent-platforms.md` |
| ② 取舍 | DESIGN §3（范围 + 迭代版图） |
| ③ 选型 | DESIGN §4 |
| ④ 运维 | DESIGN §11 |
| ⑤ 归档 | DESIGN（决策部分）+ 本文件 |
| ⑥ 架构 | DESIGN §5–§10 + CLAUDE.md（行为指令） |
| ⑦ 工程搭建 | ✅ P0-1（后端骨架：Docker/Alembic+pgvector/Celery/import-linter，一键起） |
| ⑧ 功能实现 | ✅ P0-2（core 横切 + identity 登录鉴权）；✅ P0-3（models 模型网关 + observability 基线 + 外部调用韧性）；✅ P0-4（前端控制台：Vite+React+TS+orval）；✅ P0-5（聊天助手 + SSE 流式）；✅ P0-6（RAG 知识库 + Celery 摄取 + pgvector 检索）；✅ P0-7（工具集成：builtin/api/mcp + 协议层）；✅ P0-8（ReAct Agent 循环 + step SSE 事件）；✅ P0-9（Annotation 评分 + eval_events + 增强 trace 查询） |
| ⑨⑩ 测试/部署 | 集成测试覆盖全链路（每功能阶段 2–5 tests）；部署待补（Docker Compose 配置基础存在）；见 §2bis + §2ter |

---

## 4. 待项目所有者定稿/补充
- 评估的**具体方法与指标**（数据集、判定标准、回归方式）——评估钩子已预留接口，方法待填。
- 各阶段"完成判据"的统一模板。
- 本方法论随实践的版本记录（**每完成一个阶段更新一次**）。
