# 行为规范 — {{USER_NAME}}

> 从协作历史提炼的行为规则，避免重复犯同一错误。
> 格式：规则 → **Why**（原因）→ **应用场景**

---

## 执行风格

<!-- 例：自主执行，不在任务间停下来确认 → Why: 用户希望 Agent 高效完成任务 → 应用：所有实现任务 -->

## 代码规范

<!-- 例：不引入技术栈外依赖 → Why: 保持技术栈简洁 → 应用：所有实现时 -->

## 安全规范

<!-- 例：SSE 错误不用 str(exc) → Why: 避免内部信息泄露 → 应用：所有 SSE 端点 -->

## 架构规范

<!-- 例：跨模块只走 service 接口 → Why: 强制依赖分层 → 应用：所有模块间调用 -->

## Git 规范

<!-- 例：提交前先 git diff 核对 → Why: 避免提交无关改动 → 应用：所有 commit 前 -->

## 沟通规范

<!-- 例：响应简洁，不在结尾总结已做的事 → Why: 用户能看 diff → 应用：所有响应 -->
